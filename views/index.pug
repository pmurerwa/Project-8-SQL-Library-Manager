extends layout

block content
  h1= title
  p Welcome to #{title}


//- extends layout

//- block content
//-   h1 Books Listing
//-   ul
//-     each book in books
//-       li 
//-         a(href=`/books/${book.id}`) #{book.title} by #{book.author}
//-   a(href="/books/new") Add a New Book
